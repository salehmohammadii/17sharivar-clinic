let my_checkBox=document.querySelectorAll(".my_checkBox");my_checkBox.forEach(check=>{    check.onclick=function(){        this.classList.toggle("actives");        let day=this.getAttribute("data-day");        let id=this.getAttribute("data-id");        $.ajax({            type: "POST",            url: window.hostUrl+'admin/calendar/update-weekday',            data: {user_id:id,weekDay:day},            success: function (response) {                swal("موفق", "عملیات باموفقیت انجام شد", "success");                if(response=="all"){                    setTimeout(function(){                         window.location.reload();                    },2000);                                   }            }        });    }});let online_time=document.querySelectorAll(".online_time");online_time.forEach(inputs=>{    inputs.addEventListener("dblclick",function(){      let val=this.value;      let user_id=this.getAttribute("user-id");      let day_name=this.getAttribute("day_name");      $.ajax({        type: "POST",          url: window.hostUrl+'admin/calendar/update_weekday_ontime',        data: {ontime:val,user_ids:user_id,day_names:day_name},        success: function (response) {            swal("موفق", "عملیات باموفقیت انجام شد", "success");            if(response=="all"){                setTimeout(function(){                     window.location.reload();                },2000);                           }        }    });          })})