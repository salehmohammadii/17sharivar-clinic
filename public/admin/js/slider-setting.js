let slider_actions = document.querySelectorAll(".slider-actions");let slide_btn = document.querySelectorAll(".new_slide");let input = document.querySelector(".hide_input");slider_actions.forEach(input => {    input.onclick = function () {        let attr = this.getAttribute("data-id");        let stt = this.getAttribute("status");        $.ajax({            type: "POST",            url: window.hostUrl+"admin/slider/update",            data: { actions: attr, status: stt },            success: function (response) {            }        });    }})slide_btn.forEach(btn => {    btn.onclick = function () {        let attr = this.getAttribute("data-id");        input.value = attr;    }});let del_btns = document.querySelectorAll(".del_btn");del_btns.forEach(btn => {    btn.onclick = function () {        let idVal = this.getAttribute("data-id");        $.ajax({            type: "POST",            url: window.hostUrl+"admin/slider/update",            data: { idVal: idVal },            success: function (response) {                if (response == "ok") {                    setTimeout(function () {                        window.location.reload();                    }, 500);                }            }        });    }});let disable_btn = document.querySelectorAll(".disable_btn");disable_btn.forEach(btn => {    btn.onclick = function () {        let disVal = this.getAttribute("data-id");        let statusVal = this.getAttribute("status");        $.ajax({            type: "POST",            url: window.hostUrl+"admin/slider/update",            data: { disVal: disVal, statusVal: statusVal },            success: function (response) {                if (response == "ok") {                    setTimeout(function () {                        window.location.reload();                    }, 500);                }            }        });    }});let edit_btn = document.querySelectorAll(".edit_btn");let edit_input = document.querySelector(".edit_input");edit_btn.forEach(btn => {    btn.onclick = function () {        let attr = this.getAttribute("data-id");        edit_input.value = attr;    }});