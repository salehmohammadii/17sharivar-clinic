var contact_answer_btn=document.querySelectorAll(".contact_answer_btn");let title=document.querySelector(".titles2");let input=document.querySelector(".hide_input");contact_answer_btn.forEach(btns=>{    btns.onclick=function(){            let id=btns.getAttribute("data-answer");            let name=btns.getAttribute("data-name");            title.innerHTML="  ثبت پاسخ برای کاربر "   + name ;            input.value=id;    }});let show_btn=document.querySelectorAll(".show_btn");let Show_content=document.querySelectorAll(".Show_content");show_btn.forEach(btns=>{    btns.onclick=function(){            let subject=btns.getAttribute("data-subject");            let name=btns.getAttribute("data-name");            let phone=btns.getAttribute("data-phone");            let x=0;            let Arr=Array(subject,name,phone);            Show_content.forEach(input=>{                input.value=Arr[x];                x++;            });    }});